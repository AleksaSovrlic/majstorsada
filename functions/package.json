{
  "name": "majstorsada-functions",
  "private": true,
  "//engines": "Runtime for the `api` codebase. See nuxt.config.ts (nitro.firebase.nodeVersion) for where the other runtime lives and until when a rollback to 20 is possible.",
  "engines": {
    "node": "24"
  },
  "volta": {
    "extends": "../package.json"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "node scripts/gen-version.mjs && rimraf lib && tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "lint": "echo 'no lint configured'",
    "start": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.2"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
